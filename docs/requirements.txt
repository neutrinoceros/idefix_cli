mkdocs==1.5.3
mkdocs-material==9.4.2
mkdocs-include-markdown-plugin==6.0.1
mkdocstrings[python]==0.23.0

# workaround for readthedocs + src layout
# point to the "current dir":
# assuming this is installed from the top level of the repo
# as `pip install -r docs/requirements.txt`
.
